1.  Creating Arrays

        1. Hold multiple values
            var someData = ["one", "two", "three", "four"];
        
        2. 0 based index
            var someData = new Array("one", "Two", "Three")

2.  Arrya Methods

    -- var someData = [10, 20, 30, 40, 50];
        -- var newSomeData = someData.reverse();
                                     .join();
                                     .sort();