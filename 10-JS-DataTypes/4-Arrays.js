// Working with arrays

var someData = [10, 20, 30, 50, 80, 70];

// Sorting, Reverse, Join
// var newData = someData.sort();
// var newData = someData.reverse();
var newData = someData.join();
console.log(newData);